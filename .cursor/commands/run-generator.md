# run-generator

Walk into `M3/tms-data-generator` directory (or make sure that you're there already).

Then run `task run` to generate some SQL files.

And verify the output files that it's successfully created.
